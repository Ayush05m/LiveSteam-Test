# =============================================================================
# Server Environment Configuration (Example)
# =============================================================================
# Copy this file to .env and update the values as needed.

# FFmpeg Configuration
# Windows: C:\ProgramData\chocolatey\bin\ffmpeg.exe
# Linux/Mac: /usr/bin/ffmpeg or just 'ffmpeg' if in PATH
FFMPEG_PATH=ffmpeg

# Server Port - Main API and Socket.IO server
PORT=3001

# RTMP Port - For OBS/streaming software to push to
RTMP_PORT=1935

# GPU Encoding - Set to 'true' if NVIDIA GPU is available
# Requires NVIDIA drivers and NVENC support
ENABLE_GPU=false

# Media Root Directory - Where streams and recordings are stored
MEDIA_ROOT=./media

# Node Environment
NODE_ENV=development

# =============================================================================
# Optional: Cloud Storage Configuration (for rclone)
# =============================================================================
# CLOUD_BUCKET=my-bucket
# CLOUD_REGION=us-east-1

# =============================================================================
# Production Example:
# =============================================================================
# PORT=3001
# RTMP_PORT=1935
# ENABLE_GPU=true
# FFMPEG_PATH=/usr/bin/ffmpeg
# MEDIA_ROOT=/opt/media
# NODE_ENV=production
